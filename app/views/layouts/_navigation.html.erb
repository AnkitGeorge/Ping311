<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Home', root_path, class: 'navbar-brand' %>
      <%= link_to 'Report', new_report_path, class: 'navbar-brand' %>

      <% if current_user %>
        <%= link_to "Your Page", user_path(current_user.id), class: 'navbar-brand' %>
        <%= link_to "Logout", :logout, method: :post, class: 'navbar-brand' %>
      <% else %>
        <%= link_to "Register", new_user_path,class: 'navbar-brand' %>
        <%= link_to "Login", :login, class: 'navbar-brand' %>
      <% end %>

      <%= button_tag "Use current location", id: 'current_location' %>

      <%= form_tag(reports_path, :method => 'get') do %>
        <p>Search by postal code.</p>
        <%= text_field_tag "Postal Code" %>
        <%= submit_tag "Submit", id: 'Postal_Code_Submit' %>
      <% end  %>

</div>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
    </div>
  </div>
</nav>
