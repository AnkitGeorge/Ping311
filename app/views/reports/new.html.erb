<h2>Report a Pot Hole</h2>
<%= form_for @report do |f| %>
  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <div class="field">
    <%= f.submit %>
  </div>

<h4>Reports near this location</h4>
  <ol>
    <li>
      <% @nearbyholes.each do |h| %>
      <p><%= r.location %></p>
      <p><%= r.address %></p>
      <p><%= r.status %></p>
      <p><%= r.distance_to(@report) %></p>
    </li>
  </ol>

<% end %>
